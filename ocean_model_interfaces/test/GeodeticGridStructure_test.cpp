#include "ocean_model_interfaces/geodetic_grid/GeodeticGridStructure.h"
#include <gtest/gtest.h>

using namespace ocean_model_interfaces;

GeodeticGridStructure structure("./ocean_model_interfaces/test_data/geodetic_grid", 2, 5, 10, 10);

TEST(GeodeticGridStructureTest, GetGridChunkInfo)
{
    ASSERT_TRUE(false);
}

TEST(GeodeticGridStructureTest, IsInRange)
{
    ASSERT_TRUE(false);
}

TEST(GeodeticGridStructureTest, GetWaterColumnDepth)
{
    ASSERT_TRUE(false);
}